import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
   output: 'export',
   basePath: '/nathanfeeney.github.io',  
  assetPrefix: '/nathanfeeney.github.io', 

};

export default nextConfig;
